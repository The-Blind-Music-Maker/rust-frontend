# Rust frontend for The Blind Music maker

At the moment very much in development state. This app can be used to drive The Blind Music maker api instead of the web frontend. Init config is loaded from a yaml config file and at the moment can't be changed at runtime.